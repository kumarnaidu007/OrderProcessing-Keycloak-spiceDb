# .NET Take-Home: Order Processing System

Main project documentation is in `OrderProcessing/README.md`.

## Run in one command (Docker)

From this folder:

```bash
docker compose up --build
```

API: `http://localhost:8080`  
Swagger: `http://localhost:8080/swagger`

## Additional submission docs

- `DESIGN.md`
- `EXPLANATION.md`
- `ASSUMPTIONS.md`
